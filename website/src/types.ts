export type Doc = {
  slug: string;
  title: string;
  author: string;
  date: string;
  content: string;
};

export type Donation = {
  date: string;
  name: string;
  img: string;
  web: string;
  amount: number;
};